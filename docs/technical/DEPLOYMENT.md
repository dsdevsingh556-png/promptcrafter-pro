# Deployment

This is a static project. Serve the repository root. For Vercel, import the repository and use no build command; the included rewrite sends application routes to `index.html`. After deployment, run browser, mobile, offline, IndexedDB, network and accessibility smoke tests before claiming production verification.

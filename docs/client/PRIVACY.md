# Privacy

Normal compilation runs in the browser. The shipped runtime contains no provider API keys or remote compilation endpoint. Projects saved through the interface use local IndexedDB. Selecting OpenAI, Gemini, or Grok generates instructions for that target; it does not contact that provider.

This statement applies to the supplied source. A deployment owner must repeat network inspection after adding hosting analytics or third-party scripts.

import{grounded}from'./cis.js';
const req=(id,text,category='quality',priority='must')=>({id,statement:grounded(text,'derived',`Added by ${id}`,{ruleId:id}),category,priority,verification:{method:'presence',criterion:text}});
export function applyPolicies(input,detection){const c=structuredClone(input),applied=[],trace=[];const add=(r)=>{if(!c.requirements.some(x=>x.id===r.id)){c.requirements.push(r);trace.push(`added:${r.id}`)}};c.constraints.push({id:'C-TRUTH',required:['State uncertainty'],forbidden:['Fabricate facts, citations, tools, actions or measurements'],replacements:['Use UNKNOWN or ask one blocking clarification']});applied.push('TRUTH');if(c.deliverables.length){add(req('R-OUTPUT','Define the exact output structure for every deliverable','format'));applied.push('OUTPUT')}if(detection.capabilities.includes('coding')){add(req('R-CODE','Return complete code without placeholders','functional'));add(req('R-TEST','Include validation or test instructions'));applied.push('CODING')}if(detection.capabilities.includes('research')){add(req('R-EVIDENCE','Separate sourced facts, inference, contradictions and unknowns','evidence'));add(req('R-CITATIONS','Provide citations when sources are available','evidence','should'));applied.push('RESEARCH')}if(detection.capabilities.some(x=>['data','extraction'].includes(x))){add(req('R-MISSING','Define how unavailable values are represented','format'));applied.push('DATA')}if(detection.capabilities.includes('agent')){add(req('R-AGENT','Define tools, permissions, action boundaries and stopping conditions','process'));add(req('R-RECOVERY','Define recovery and human approval gates','safety'));applied.push('AGENT')}c.provenance.push({ruleId:'POLICY-1.0',action:`applied ${applied.join(',')}`});return{cis:c,applied,trace}}

import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join,normalize}from'node:path';const root=process.cwd(),port=4173,types={'.html':'text/html','.js':'text/javascript','.css':'text/css','.json':'application/json','.webmanifest':'application/manifest+json','.svg':'image/svg+xml'};createServer(async(req,res)=>{try{let p=decodeURI(req.url.split('?')[0]);if(p==='/')p='/index.html';let f=normalize(join(root,p));try{if(!(await stat(f)).isFile())f=join(root,'index.html')}catch{f=join(root,'index.html')}res.setHeader('Content-Type',types[extname(f)]||'application/octet-stream');res.end(await readFile(f))}catch(e){res.statusCode=500;res.end('Server error')}}).listen(port,()=>console.log(`PromptCrafter Pro: http://localhost:${port}`));
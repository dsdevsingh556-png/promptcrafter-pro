import{mkdir,rm,cp,readFile,writeFile}from'node:fs/promises';const root=new URL('../',import.meta.url),dist=new URL('../dist/',import.meta.url);await rm(dist,{recursive:true,force:true});await mkdir(dist,{recursive:true});for(const p of ['index.html','styles.css','manifest.webmanifest','sw.js','favicon.svg','icon.svg','src'])await cp(new URL('../'+p,import.meta.url),new URL('../dist/'+p,import.meta.url),{recursive:true});console.log('Built static Vercel output in dist/');